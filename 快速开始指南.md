# 快速开始指南

## 重要提示：缺失的文件

项目结构已完整创建，但有一个二进制文件需要你自己获取：

### `gradle/wrapper/gradle-wrapper.jar`

这个是 Gradle Wrapper 的可执行 jar 文件，无法通过文本方式创建。你有以下几种方式获取：

### 方式一：从 Android Studio 项目复制（推荐）

1. 在 Android Studio 中创建任意一个新的 Android 项目
2. 从新项目的 `gradle/wrapper/` 目录下复制 `gradle-wrapper.jar` 文件
3. 粘贴到本项目的 `gradle/wrapper/` 目录下

### 方式二：手动下载

```bash
cd 抖音看图_完整版/gradle/wrapper
# 下载 gradle-wrapper.jar (版本 8.2)
curl -L -o gradle-wrapper.jar https://services.gradle.org/distributions/gradle-8.2-bin.zip
# 解压并提取 gradle-wrapper.jar
unzip gradle-8.2-bin.zip
cp gradle-8.2/lib/gradle-wrapper-8.2.jar gradle-wrapper.jar
rm -rf gradle-8.2 gradle-8.2-bin.zip
```

### 方式三：不使用 wrapper，直接使用系统 gradle

如果你本地安装了 Gradle 8.2+，可以直接运行：
```bash
gradle assembleDebug
```

---

## 完整的项目设置步骤

### 1. 准备 gradle-wrapper.jar

按照上面的说明获取 `gradle-wrapper.jar` 文件。

### 2. 在 Android Studio 中打开项目

```
File -> Open -> 选择 "抖音看图_完整版" 文件夹
```

### 3. 等待 Gradle 同步

Android Studio 会自动：
- 下载 Gradle 8.2（如果没有）
- 下载所有依赖库
- 索引项目文件

### 4. 同步完成后

- 检查 Build 窗口是否有错误
- 如果有红色错误，请参考下方"常见问题"

### 5. 运行应用

- 连接 Android 设备 或 启动模拟器
- 点击 Run 按钮（绿色三角形）
- 或使用快捷键 `Shift + F10`

---

## 首次运行须知

### 权限请求

应用首次启动会请求存储权限：
- Android 13+：请求图片和视频媒体权限
- Android 12-：请求读写外部存储权限

**必须授予权限**才能正常浏览媒体文件。

### 加载媒体

授予权限后，应用会自动扫描手机中的所有图片和视频：
- 图片：JPG、PNG、WEBP、GIF 等
- 视频：MP4、MKV、AVI（取决于设备支持）

---

## 常见构建问题

### 问题 1：Gradle 同步失败

**错误信息**：`gradle-wrapper.properties` 找不到 jar

**解决方法**：确保已经添加了 `gradle/wrapper/gradle-wrapper.jar` 文件

---

### 问题 2：SDK 版本不匹配

**错误信息**：`compileSdkVersion 34 not found`

**解决方法**：
1. 打开 SDK Manager
2. 安装 Android 14 (API Level 34)
3. 重新同步 Gradle

---

### 问题 3：Kotlin 版本兼容

**错误信息**：Kotlin 版本不兼容

**解决方法**：
项目使用 Kotlin 1.9.22，确保 Android Studio 插件已更新。

---

### 问题 4：ViewBinding 未生成

**错误信息**：`ActivityMainBinding not found`

**解决方法**：
```
Build -> Clean Project
Build -> Rebuild Project
```

---

## 预期的项目结构

正确配置后，项目应该包含：

```
抖音看图_完整版/
├── gradle/
│   └── wrapper/
│       ├── gradle-wrapper.jar      ✅ 需要添加
│       └── gradle-wrapper.properties
├── app/
│   ├── src/
│   │   └── main/
│   │       ├── AndroidManifest.xml
│   │       ├── java/               ✅ 所有 Kotlin 代码
│   │       └── res/                ✅ 所有资源文件
│   └── build.gradle.kts
├── build.gradle.kts
├── settings.gradle.kts
├── gradle.properties
├── gradlew
├── gradlew.bat
└── README.md
```

---

## 验证构建成功

构建成功后，你会在以下位置找到 APK：

```
app/build/outputs/apk/debug/app-debug.apk
```

可以直接安装到 Android 设备上测试：
```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```

---

## 开发建议

### 推荐使用的 Android Studio 版本
- Android Studio Hedgehog (2023.1.1) 或更高版本
- 包含 Kotlin 1.9+ 插件
- 支持 Gradle 8.x

### 调试技巧
1. 使用 Logcat 查看日志
2. 设置断点调试
3. 使用 Layout Inspector 检查界面

---

## 需要帮助？

如果遇到问题：
1. 检查是否遗漏了 `gradle-wrapper.jar`
2. 确认所有 SDK 和依赖版本正确
3. 查看 `README.md` 了解更多细节

祝使用愉快！ 🚀
